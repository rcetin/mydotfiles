separator_block_width=15
markup=none

[volume]
command=$SCRIPT_DIR/volume/volume
label=🔊 
interval=10
signal=10

[memory]
command=$SCRIPT_DIR/memory/memory
label=MEM 
separator=true
interval=30

[cpu_usage]
command=$SCRIPT_DIR/cpu_usage/cpu_usage
label=CPU 
interval=10
min_width=CPU 100.00%
#separator=false

[memory]
#label=SWAP
#instance=swap
#separator=false
#interval=30

[disk]
command=$SCRIPT_DIR/disk/disk
label=HOME 
#DIR=/mnt/data
interval=180

[bandwidth]
command=$SCRIPT_DIR/bandwidth2/bandwidth2
interval=persist
markup=pango
label=↓↑
WARN_RX=307200
WARN_TX=30720
CRIT_RX=512000
CRIT_TX=51200

# Generic media player support
#
# This displays "ARTIST - SONG" if a music is playing.
# Supported players are: spotify, vlc, audacious, xmms2, mplayer, and others.
[mediaplayer]
command=$SCRIPT_DIR/mediaplayer/mediaplayer > /dev/null 2>&1
instance=spotify
interval=5
signal=10

# Query my default IP address only on startup
[ip]
command=/sbin/ip -o -4 addr list wlp2s0 | awk '{print $4}' | cut -d/ -f1
interval=once
color=#91E78B

# Date Time
[time]
label=🕓 
command=date '+%Y-%m-%d %H:%M'
interval=30
